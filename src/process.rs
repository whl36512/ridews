/*
extern crate websocket;

use self::websocket::message::{OwnedMessage};

pub trait ProcessMsg{
    fn process_msg(msg: OwnedMessage) -> Option<OwnedMessage> {
        Some(msg)
    }

    fn process_msg(&self : OwnedMessage) -> Option<OwnedMessage> {
        Some(self)
    }
}
*/

